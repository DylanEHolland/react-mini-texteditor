import React from 'react';
import ReactDOM from 'react-dom';

import TextEditor from "./react-mini-texteditor";

ReactDOM.render(
    (
        <React.StrictMode>
            <TextEditor />
        </React.StrictMode>
    ),
    document.getElementById("root")
)